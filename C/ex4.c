#include <stdio.h>
int main ()
{
   double hours,amount,salary;
   printf("employe number=25\n");
   printf("entar the hours");

   scanf("%0.2if",&hours);

   printf("enter the amount");
   scanf("%0.2if", &amount);
    salary = hours*amount ;


    printf("SALARY= %0.2if",salary);
   return 0;
}
